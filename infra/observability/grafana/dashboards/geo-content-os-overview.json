{
  "annotations": { "list": [] },
  "editable": false,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 1,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum by (le, route) (rate(geo_api_request_duration_seconds_bucket[5m])))",
          "legendFormat": "{{route}}",
          "refId": "A"
        }
      ],
      "title": "API latency P95",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        { "expr": "geo_queue_lag_jobs", "legendFormat": "{{queue}} lag", "refId": "A" },
        {
          "expr": "geo_queue_oldest_job_age_seconds",
          "legendFormat": "{{queue}} oldest seconds",
          "refId": "B"
        }
      ],
      "title": "Queue lag and oldest job",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum by (provider, model, skill) (increase(geo_ai_cost_cents_total[1h]))",
          "legendFormat": "{{provider}} / {{model}} / {{skill}}",
          "refId": "A"
        }
      ],
      "title": "AI cost per hour (cents)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "max": 100, "min": 0, "unit": "percent" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "100 * sum by (platform) (rate(geo_publish_attempts_total{status=\"success\"}[5m])) / clamp_min(sum by (platform) (rate(geo_publish_attempts_total[5m])), 0.001)",
          "legendFormat": "{{platform}}",
          "refId": "A"
        }
      ],
      "title": "Publish success rate",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 0, "y": 16 },
      "id": 5,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"], "values": false } },
      "targets": [
        {
          "expr": "sum(rate(geo_api_requests_total{status_class=\"5xx\"}[5m])) / clamp_min(sum(rate(geo_api_requests_total[5m])), 0.001)",
          "refId": "A"
        }
      ],
      "title": "API 5xx rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "percentunit" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 8, "y": 16 },
      "id": 6,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"], "values": false } },
      "targets": [
        {
          "expr": "sum(rate(geo_ai_schema_results_total{result=\"failed\"}[5m])) / clamp_min(sum(rate(geo_ai_schema_results_total[5m])), 0.001)",
          "refId": "A"
        }
      ],
      "title": "AI schema failure rate",
      "type": "stat"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "short" }, "overrides": [] },
      "gridPos": { "h": 7, "w": 8, "x": 16, "y": 16 },
      "id": 7,
      "options": { "reduceOptions": { "calcs": ["lastNotNull"], "values": false } },
      "targets": [
        {
          "expr": "sum(increase(geo_publish_attempts_total{status=\"unknown\"}[5m])) + sum(increase(geo_outbox_terminal_failures_total[5m]))",
          "refId": "A"
        }
      ],
      "title": "Unknown publish and terminal outbox events",
      "type": "stat"
    }
  ],
  "refresh": "30s",
  "schemaVersion": 42,
  "tags": ["geo-content-os", "mvp"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "GEO Content OS Overview",
  "uid": "geo-content-os-overview",
  "version": 1
}
