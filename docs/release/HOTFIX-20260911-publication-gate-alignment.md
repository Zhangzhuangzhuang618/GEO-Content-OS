# 2026-09-11 日批发布门禁对齐（已部署）

## 生产证据

北京时间 04:47，生产 72d74c9 的今天日批中，58 篇已创建发布任务但未发布，38 篇未通过只读内容门禁回放。官网 18/20，列举网 20/20；抖音 0/13，百家号 0/5。另有 6 个候选在生成质检/改写阶段失败，无发布任务，不混入上述计数。

- 众人官网 7 篇、众人列举网 4 篇引用了正文未介绍的证照。
- 官网 16 篇触发旧的 800 字下限（有效字数 573–791）；与证照失败有重叠。
- 列举网 20 篇的 `platform_meta.content_type` 为 null，渲染要求 `logistics_freight`。

原始只读结果：`tmp/daily-audit-0911-result.json`。检查不包含远程平台提交、实时浏览器登录态验证，因此静态通过不等于发布成功。

## 原因和修复

证照链路存在“可用资料”和“实际引用”混用：旧版改写重建曾挂上公司的全部绑定来源；72d74c9 已改为保留原引用，但会继承历史污染。选材检查要求已选证照必须写入正文，而最终装配只检查引用归属，没有清理反向的“已引用但未介绍”。企业保障补句在选中证照已覆盖时又会省略其他绑定证照；发布端根据实际引用要求证照名称完整出现，因而报 COPY_INVALID。

本次在历史稿重建和最终装配两处统一清理：仅对明确标记为企业证照且具有证照名称的来源，剔除本公司推荐段没有介绍的证照引用。服务和保险引用保留；不修改原稿对象。外部/无效引用仍校验，过滤后无引用的推荐段拒绝，选中证照的名称与引用一致性检查仍在。不通过向文章补一串证照名单解决问题。

官网：尊重既定“约 1100 字是提示，不设硬广告最低/最高字数”的要求，仅有效硬广风格快照跳过旧正文长度门禁。常规文章 800–2500 字及其他发布要求保持。

列举网：硬广内容装配显式写入 `content_type: logistics_freight`，不放宽发布 Schema。

## 验证和边界

AI 全量 422 项、官网/列举网渲染 27 项通过；AI 和平台适配器类型检查通过。新增历史污染引用清理、实际证照保留、无依据拒绝、列举网内容类型、官网硬广与常规字数策略分离用例。

## 部署与历史稿处理结果

北京时间 05:20 核验：生产 API、AI Worker、Publisher Worker 已运行 `8c6fcf0191c92a4c237d17cb87813d5298bf74df`；Web 无本次代码变更，保留 `72d74c9` 镜像。16 个生产容器 healthy，首页及 API readiness 均为 200。未执行数据库迁移，原有生产配置和用户工作区改动保留。

构建 Web 镜像期间 Docker 引擎失联，首页及 API 一度超时。SSH 可用，主机内存和磁盘充足，尚不能确认最初引擎故障根因。SSH 发起的 Desktop restart 未保持后台进程；随后在已有 administrator 交互会话中启动 Docker Desktop，Hyper-V 虚拟机和所有容器恢复。没有重置 Docker、删除卷或数据库。故障期间没有切换新镜像；恢复后仅切换上述三个后端镜像。

数据库备份：`D:\GEO-Backups\HOTFIX-20260911-gate-repair\database.dump`，SHA256 `31E9991D189AEBBA18B0A7E33B8C4D9A61281D02A707563A37618597DFF6BE22`；备份已通过 pg_restore 列表验证。配置、旧镜像标记及工作区补丁保留在同一备份目录；部署目录 `D:\GEO-Content-OS-Releases\gate-0911-8c6fcf0`。

- 原 38 篇受阻稿：36 个 scheduled 任务先经业务服务暂延，另 2 个 failed 未盲目重试。
- 28 篇创建新版本（官网 8、列举网 20），仅清理未在对应公司段落使用的证照引用及补齐列举网类型；正文 blocks 全部与旧版本相同。包括 1 篇额外清理了未触发原证照门禁的冗余证照引用。
- 旧版本、旧质检和失败尝试保留；28 个旧任务经取消流程退出。新版本通过既有 manual_edit 流程真实复验，不复制旧质检报告，不触发自动改写。
- 16 篇新版本复验通过（官网 5、列举网 11），12 篇返回 revise（官网 3、列举网 9），后者保留待人工处理，未放行。模型反馈不自动等同于已证实的内容缺陷。
- 10 篇仅需新门禁、无需更改内容；加上 16 篇通过修订稿，共 26 篇经业务服务恢复原未来排期。无须过期补排，临时延后任务残留为 0。
- 当前 46 篇待发布稿只读发布门禁全部通过（官网 17、列举网 11、抖音 13、百家号 5）。此结论不涵盖外部平台提交和实时浏览器登录态，不等同于已发布成功。
- 原 6 篇生成/质检失败候选未放行；与上述 12 篇分开统计。未以凑足数量为由改写或绕过质检。

审计 request_id：`gate-0911-hold`（36）、`gate-0911-revision`（含 28 个 publication_metadata_repaired 事件）、`gate-0911-restore`（26）。历史修订与恢复均先事务回滚演练，再正式执行。
