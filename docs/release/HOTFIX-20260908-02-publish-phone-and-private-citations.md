# HOTFIX-20260908-02 官网双电话兼容与抖音私有资料引用

用户授权：众人官网兼容 02085627757 / 4008372383，修复抖音私有资料引用发布缺口。

## 生产证据

- 2026-09-08 13:48:11（北京时间），众人工作区服务电话由 02085627757 改为 4008372383。
  已排期内容仍保存旧号码，14:00 / 15:30 / 17:00 的发布在 SERVICE_PHONE_REQUIRED 处失败。
- 志远抖音客户口吻 08:00 的稿件引用有效服务说明 TXT，ai_citations 关联存在，但私有 s3 原件
  没有公开 URL。Publisher 只装载 HTTP(S) 链接，Douyin Render 因 CITATION_LINK_MISSING 阻断。
  此稿 7 张媒体齐全，账号后续发布正常，不是登录失效或图片故障。

## 修改

- 新增 Publisher 配置 OFFICIAL_SITE_COMPATIBLE_SERVICE_PHONES_JSON，默认空对象。
  格式为账号 UUID 到最多两个已确认服务号码的映射，仅影响该账号的发布阶段。
- 当前企业主号码必须仍在允许列表中，且原文已满足“CTA 中出现、全文恰好一次”的原门禁，
  才可选择对应号码渲染。不改写不可变内容版本，不改变企业主号码或生成逻辑，不接受任意历史号码。
- 众人官网生产配置：
  `{"e4ff285d-7df4-4fec-883c-322a1648006a":["02085627757","4008372383"]}`。
- 抖音私有引用只接受已关联当前内容版本、同租户、同工作区、同项目或工作区公共资料，
  且文档/分块 active、未删除、在有效期内的 PDF / DOCX / TXT / 图片资料。
- Publisher 从数据库核验私有引用，向 Render 传递可选 internal_citation_ids；仍要求每条引用
  对应公开链接或已核验私有资料。该字段不会进入发布 Payload、正文或导出文件，也不创建伪造 URL。
- 无数据库迁移、公开 API、Web 或 AI Worker 变化。不改变文章事实/质量门禁、不重跑历史任务。

## 验证

- 平台 Render 契约 93 项、Publisher 单元 23 项、独立 PostgreSQL 集成 30 项通过。
- 覆盖账号隔离、任意旧号拒绝、主号码未来变更后兼容失效、CTA/重复电话门禁、原文不变；
  覆盖私有引用有效/过期/未生效/已删除/未关联/跨项目/跨工作区及公开链接继续输出。
- 平台和 Publisher 类型检查、Publisher 生产构建及修改文件 ESLint / 格式检查。
- 部署后使用生产内容进行只读渲染验证；不得为验证而调用真实发布或 claim 写入流程。

## 部署与回滚

- 使用干净提交归档构建，仅 no-deps 更新 publisher-worker。
- 保留旧镜像、原始发布日志和受保护的 .env 备份；服务器既有环境改动和数据卷不动。
- 仅向生产 .env 增加上述账号级非秘密配置，原有配置不覆盖；检查新旧容器环境差异。
- 回滚使用保留的旧 Publisher 镜像。若撤销兼容策略，只移除新增的配置行，不回退整份 .env。
- 已失败任务保持原状态；新排期按新规则执行。需要补发的历史任务另行获得明确授权。
