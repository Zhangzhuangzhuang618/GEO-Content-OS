# HOTFIX-20260806-13 百家号编辑器列表规范化

## 真实页面证据

- HOTFIX-20260806-12 使用真实键盘事件后，百家号编辑器字数由 0 更新为非零，正文已进入平台内部状态。
- 提交前完整性检查发现两个确定性差异：每个编号清单的首项 `1. ` 被转换为 `<ol>` 结构；退出第二个列表时编辑器插入一个不可见的 U+200D 格式字符。
- 实际页面存在 2 个 `<ol>`、11 个 `<li>`，与原文的两个编号清单及 11 个清单项一致；移除两个首项标记和 Unicode 格式控制字符后，其余正文逐字一致。
- 本轮诊断均在发布前停止，没有点击发布；此前提交后也已确认“全部、已发布、待发布、未通过、已撤回、草稿”六个状态均无目标文章。

## 修复

- 正文完整性校验继续优先接受逐字一致。
- 对百家号确定性的自动列表转换，仅在规范化文本等于“移除每个行首 `1. `”后的原文，且实际 `<ol>` 数量与原文清单起始数量完全一致时接受。
- 比较时忽略编辑器生成的 Unicode `Cf` 格式控制字符；不忽略普通文字、数字、标点或空白分段之外的内容。

## 安全边界

- 不使用包含匹配、相似度或长度近似代替正文完整性校验。
- 列表数量不一致、正文字符缺失或新增时仍在发布点击前阻断。
- 不修改质量门禁、图片门禁、发布幂等或未知态策略。

## 部署与验收

- 重新构建并部署 Baijiahao Browser Worker；无需数据库迁移。
- 浏览器回归模拟 `<ol>` 自动转换和 U+200D 插入，验证结构和全文同时通过后才允许提交。
- 真实发布前截图必须显示非零字数、完整正文和已就绪封面。
