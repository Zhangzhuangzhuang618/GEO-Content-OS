# T165：三平台硬广联系电话

当前状态：2026-09-11 22:52 已部署产品提交 `982cbabc0728060ee9e42cb9dc193e83032e2394`，十个账号配置完成。下文“部署暂停”保留此前事故过程，不代表当前状态。

## 范围

官网、列举网、抖音的推荐公司新增可选 `service_phone`，复用既有大陆手机号、座机、400/800 格式验证。主公司电话只从当前租户/工作区企业资料 `official_site_service_phone` 读取，不在推荐配置重复维护；关联公司单独保存。账号默认风格、日批覆盖和已保存常规口吻不变。

生成候选解析企业资料时冻结主公司有效电话，关联公司电话随账号配置冻结。正文由服务器在对应公司介绍后添加独立联系段，抖音发布主文案在对应公司段内同步添加；不要求模型生成号码，不把电话混入服务引用。官网原有主公司 CTA 保留。人工编辑沿用当前版本电话快照，不回填最新配置。既有无电话快照仍兼容，不修改历史文章和排期。

生成质检与发布门禁仅排除正确位置、完全匹配冻结配置的联系段后运行原有电话检查；缺失、移用、未配置号码仍拒绝。列举网发布输入接收服务端风格快照，不向平台输出配置对象。常规风格保留原限制。

没有新增表或数据库迁移；更新 JSON 契约、OpenAPI、账号设置页面及共享校验。Web 构建并发限制为 1，避免共享生产 Docker VM 同时启动多个静态页面构建进程。

## 本地验证

- Contracts 201 项、AI Worker 426 项、平台适配器 201 项、API 单元 286 项、Skills 96 项通过。
- 2 项真实浏览器页面测试通过：主公司继承只读、关联公司号码保存及原风格字段保留。第一次浏览器启动受 macOS 沙箱限制，获准沙箱外重跑后通过。
- 新增手动/日批共用上下文解析测试：主公司不接受覆盖、关联公司不继承主公司号码、已解析快照不随后续配置改变。
- API 产品源码、AI、平台适配器、Web 类型检查及相关构建通过；API 全量测试类型检查仍报未改动测试文件中的 `PublishClaim.accountId`、跨 rootDir、unknown/JsonValue 等错误，未将该命令记为通过，未顺带修改那些测试。
- 验证包括系统实际装配、确定性质检与发布载荷；未重新调用模型生成全文，未向外部平台发布测试文章，不等同于平台审核通过。

## 生产配置计划

生产已核对主公司电话：众人 4008372383、志远 02085627757。现有十个三平台账号的关联公司配置填入：志远 02085627757、众人 4008372383、盛源 18148943200。六个硬广默认账号立即用于后续新任务，四个客户/师傅账号保留 standard，仅单次选择硬广时使用这些电话。

## 部署暂停与生产恢复（2026-09-11 20:43）

功能尚未部署、电话配置尚未写入。产品提交 `a42c617`，上线前再修正抖音完整性检查误用去电话副本的问题，提交 `982cbab`；426 项 AI 测试重新通过。其余三组件源码未因此改变。

本次前置检查只核对 Windows D 盘（剩余约 100 GB）与内存，遗漏 Docker VM 内部文件系统容量。四镜像串行构建时内部 `/dev/sda1` 写满，Redis 报 AOF `No space left on device`，PostgreSQL checkpoint 写失败并反复恢复；API readiness 一度仍返回 200，不能据此认定数据库正常。新服务尚未切换。

已停止继续部署，按明确 tag 和 cache ID 删除本次未上线的四个镜像及对应运行层缓存；均可从提交重建。未执行 system/volume prune，未删除数据库、Redis、MinIO 数据卷，也未动在用或旧版回滚镜像。20:43 PostgreSQL `SELECT now()` 成功，Redis `aof_last_write_status`、`aof_last_bgrewrite_status`、`rdb_last_bgsave_status` 均 `ok`，16 容器 healthy。Docker 文件系统 146.6 GiB，使用 137.6 GiB，可用仅 1.5 GiB，不能继续构建。

备份：`D:\GEO-Backups\T165-20260911-phones\database.dump`，SHA256 `3F750FAB7F0FBCE5F68D24C06DC273874A317EECDBB56C76679ECC42DA37F53B`，已用 pg_restore 列表验证。同目录保留配置副本、配置哈希、工作区 patch；原运行镜像另标记 `rollback-phone-0911`。运行版本仍 API/AI/Publisher `8c6fcf0`、Web `72d74c9`。

生产源码经 bundle 快进同步；GitHub 推送失败（服务器代理 ghproxy.net 非交互鉴权失败），不将远端同步记为完成。继续前须获得历史构建缓存/停用镜像的清理授权并检查内部剩余空间。不要执行旧 `tmp/phone-0911-switch.ps1`：它仍指向已删除的 `a42c617` 镜像，后续必须从最终提交重新构建并核对镜像版本。

## 获准清理、部署完成（2026-09-11 22:52）

- 用户明确允许清理本项目停用镜像和历史构建缓存，保留在用、回滚及全部数据卷。按容器引用和镜像标签核对后，删除 9 个旧 GEO 标签（对应 7 个镜像），分批清理已核实的 GEO 缓存链；未使用全局 prune，未触碰其他项目。缓存与镜像可重新构建，数据卷未删除。Docker VM 可用空间由约 1.1 GiB 增至 22.1 GiB。
- 四组件串行构建，逐步核对真实 PostgreSQL 查询、Redis 持久化、API 和内部磁盘。AI 的 Debian 字体下载缓慢，停止该构建后复用已核对的 `rollback-phone-0911` AI 运行环境；锁文件、运行 Dockerfile 无变化且无删除文件，再覆盖新编译代码。四镜像均标记最终产品提交 `982cbab`。构建后可用约 13.1 GiB。
- 新发布目录 `D:\GEO-Content-OS-Releases\phone-0911-982cbab`，镜像 `geo-content-os/{api,ai-worker,publisher-worker,web}:phone-0911-982cbab`。切换前无正在发布任务，也无非历史遗留的运行中生成任务。只替换四个应用容器，无迁移，基础服务未重启。旧回滚镜像及数据库备份保留。`tmp/phone-0911-switch.ps1` 已更新为最终版本，不能按前一节旧状态执行。
- 六个硬广默认账号各取一篇生产现有文章，只读模拟添加电话，执行实际证据门禁与平台渲染，6/6 通过，三家电话均存在于最终发布载荷。未改原文章，未调用外部发布接口，不能据此声称平台审核通过。
- 十个账号先事务回滚预演，再通过 `AccountContentPolicyService.saveInTransaction` 原子保存。审计 request ID `t165-phone-0911-configure`；真实资料解析验证主公司继承及关联电话。回读对比确认只改变电话和版本，六个硬广、四个常规默认风格不变，所有业务说明和资料绑定不变。
- 众人 `4008372383`、志远 `02085627757`、盛源 `18148943200`。主公司号码不重复保存，从企业资料继承；关联公司号码在账号推荐公司配置维护。仅后续新硬广任务采用新快照，未补写历史文章或修改排期。
- 最终四服务镜像 revision 均为 `982cbab`，16 容器 healthy，API/Web 200，PostgreSQL 查询成功，Redis 三项持久化状态 ok；应用最近日志扫描未见 error/fatal。GitHub 远端仍未完成同步，本轮通过已校验 bundle 的生产源码构建部署。
