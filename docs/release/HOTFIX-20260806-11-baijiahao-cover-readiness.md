# HOTFIX-20260806-11 百家号封面上传完成态

## 真实发布证据

- 本地真实百家号发布冒烟已完成登录、内容管理同标题预查、标题正文填充和封面上传。
- 发布前截图显示封面区域仍在转圈；随后点击发布没有创建文章，发布后核验返回 `PUBLISH_STATE_UNKNOWN`。
- 对同一真实编辑器执行仅上传不发布的诊断：封面弹窗关闭时页面仍存在 `cheetah-spin-spinning`，约 2 秒后才出现 `coverWrapper` 中的 `coverImg` 预览。
- 发布后已通过标题搜索检查“全部、已发布、待发布、未通过、已撤回、草稿”六个状态，均不存在目标文章，可以排除本次点击已经创建内容。

## 修复

- 封面文件设置并确认后，必须等待百家号编辑器出现真实封面预览，才能继续执行发布前截图和发布点击。
- 直接文件输入和“选择封面 → 本地上传”弹窗两条路径使用同一完成态校验。
- 超时未出现预览时返回明确的 `PAGE_SIGNATURE_CHANGED`，停止在发布点击之前。

## 安全边界

- 不用固定延时伪装上传成功；必须观察页面真实预览签名。
- 不把弹窗关闭、上传请求返回或加载动画存在当作封面就绪。
- 不改变质量门禁、发布幂等、未知态和人工处理策略。

## 部署与验收

- 重新构建并部署 Baijiahao Browser Worker；无需数据库迁移。
- 回归场景模拟封面弹窗先关闭、预览延迟出现；驱动必须等到预览后才点击发布。
- 真实灰度仍应先按标题预查，且一次只允许一个提交动作。
