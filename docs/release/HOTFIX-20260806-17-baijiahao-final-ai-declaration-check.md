# HOTFIX-20260806-17 百家号 AI 声明最终态校验

## 真实页面证据

- HOTFIX-20260806-16 后，驱动成功勾选 AI 声明并继续到发布点击，但回查结果为未知态。
- 发布前完整截图显示正文 1109 字、封面就绪、列表编号正常，但“采用AI生成内容”最终仍是未选中状态。
- 调用顺序显示 AI 声明在摘要、标签和指纹之前勾选；百家号后续表单更新会重新渲染并重置该受控复选框。
- 未知态后只读搜索“全部、已发布、待发布、未通过、已撤回、草稿”六个状态，均不存在目标标题，因此不得判定为已发布。

## 修复

- 将 AI 声明勾选移动到摘要、标签和指纹全部填写完成之后。
- 发布前截图持久化完成后、点击发布之前再次执行同一必需勾选与原生 `checked` 校验。
- 浏览器仿真增加回归条件：标签输入会重置 AI 复选框，确保调用顺序倒退时测试失败。

## 安全边界

- 不伪造 DOM、组件状态或平台返回结果。
- AI 声明最终态不满足时继续在发布点击前阻断。
- 不放宽正文、图片、质量、幂等或未知态门禁。

## 部署与验收

- 重新构建并部署 Baijiahao Browser Worker；无需数据库迁移。
- 单元测试和浏览器回归必须保持通过。
- 再次真实提交前必须先搜索全部作品状态；发布前截图必须显示 AI 声明已选中。
